# tmdb-cli-tool
 This is a command-line interface (CLI) tool that allows you to fetch movie information from The Movie Database (TMDB) API and display it in the terminal. You can retrieve popular, top-rated, upcoming, and now playing movies by passing a command-line argument to the tool.
